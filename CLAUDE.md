# ProxyPool - Project Instructions

## Development Environment

- **Use `uv` for all Python operations** — running tests, installing deps, executing scripts
  - `uv run pytest ...` instead of `python -m pytest ...`
  - `uv run python ...` instead of `python ...`
